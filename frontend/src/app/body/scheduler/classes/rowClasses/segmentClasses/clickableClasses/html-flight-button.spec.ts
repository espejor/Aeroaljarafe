import { HtmlFlightButton } from './html-flight-button';

describe('HtmlFlightButton', () => {
  it('should create an instance', () => {
    expect(new HtmlFlightButton(null,0)).toBeTruthy();
  });
});
