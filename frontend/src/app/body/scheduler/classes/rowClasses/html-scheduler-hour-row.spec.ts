import { HtmlSchedulerHourRow } from './html-scheduler-hour-row';

describe('HtmlSchedulerHourRow', () => {
  it('should create an instance', () => {
    expect(new HtmlSchedulerHourRow(0,null)).toBeTruthy();
  });
});
