import { HtmlSchedulerTimeSegment } from './html-scheduler-time-segment';

describe('HtmlSchedulerTimeSegment', () => {
  it('should create an instance', () => {
    expect(new HtmlSchedulerTimeSegment(null,0)).toBeTruthy();
  });
});
