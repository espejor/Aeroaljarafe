import { HtmlSchedulerGrid } from './html-scheduler-grid';

describe('HtmlSchedulerGrid', () => {
  it('should create an instance', () => {
    expect(new HtmlSchedulerGrid()).toBeTruthy();
  });
});
